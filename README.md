# Software Architecture Runtime Monitoring System

## Documentation Naming

Tracked documents under `docs/` use a three-digit prefix that follows the
original commit-introduction order. Existing documents were renamed with
spaced prefixes (`001`, `010`, `020`, ...), leaving room for future inserts
when needed.

When renaming tracked documents, use `git mv` and update any markdown links or
path references in the same change.
