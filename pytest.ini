[pytest]
addopts = -p no:cacheprovider
testpaths = tests
tmp_path_retention_policy = all
tmp_path_retention_count = 50
markers =
    linux_agent_ssh: SSH로 Linux agent test server에 접속해 실제 agent를 기동하는 통합 테스트
